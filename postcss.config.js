// postcss.config.js — Tailwind CSS v4 menggunakan plugin @tailwindcss/postcss
// autoprefixer dan postcss-import sudah built-in di Tailwind v4 (via Lightning CSS)
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
